import 'package:flutter/material.dart';

class CoverLetterWidget extends StatelessWidget {
  final Size size;
  const CoverLetterWidget({Key? key, required this.size}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
